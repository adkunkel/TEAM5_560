<Page x:Class="UserInterface.AlterDatabase"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UserInterface"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AlterDatabase">

    <Grid Background="WhiteSmoke">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Grid.Row="0" Grid.Column="0" FontSize="48" Margin="10" Click="InsertButton">
            Insert
        </Button>
        <Button Grid.Row="0" Grid.Column="1" FontSize="48" Margin="10" Click="UpdateButton">
            Update
        </Button>
        <Button Grid.Row="1" Grid.Column="0" FontSize="48" Margin="10">
            Delete
        </Button>
        <Button Grid.Row="1" Grid.Column="1" FontSize="48" Margin="10" Click="HomeButton">
            Main Window
        </Button>
    </Grid>
</Page>
